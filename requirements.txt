# ── Telegram ──────────────────────────────────────────────────────────────────
python-telegram-bot==22.7          # Python 3.11+, Windows OK

# ── Claude / Anthropic ────────────────────────────────────────────────────────
anthropic==0.104.1                 # поддерживает claude-sonnet-4-6 / claude-opus-4-7

# ── HTTP / async ──────────────────────────────────────────────────────────────
aiohttp==3.13.5                    # Python 3.11+, Windows OK (wheels есть)
# asyncio — НЕ устанавливать! встроен в Python 3.4+; PyPI-пакет — устаревший бэкпорт

# ── Окружение ─────────────────────────────────────────────────────────────────
python-dotenv==1.2.2               # чтение .env-файлов

# ── Логирование ───────────────────────────────────────────────────────────────
loguru==0.7.3                      # Python 3.11+, Windows OK

# ── Память агентов (Redis) ────────────────────────────────────────────────────
redis==5.0.1                       # включает redis.asyncio; Railway Redis plugin совместим

# ── Веб-поиск (Каспер) ────────────────────────────────────────────────────────
tavily-python==0.7.24              # AsyncTavilyClient для нативного async поиска

# ── PostgreSQL (task queue) ───────────────────────────────────────────────────
asyncpg==0.29.0                    # нативный async Postgres драйвер для task queue

# ── Голосовые сообщения (Groq Whisper) ───────────────────────────────────────
groq==1.5.0                        # AsyncGroq — транскрипция voice → text через Whisper

# ── Ева — чтение Telegram-каналов через MTProto ───────────────────────────────
telethon==1.44.0                   # MTProto клиент; StringSession совместим с asyncio
