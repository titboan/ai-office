# AI Office — навигация по проекту

Карта проекта. Читай в начале каждой сессии.
**AI Office** — команда из 9 агентов на Claude API, управляемых через Telegram, для автоматизации маркетплейс-бизнеса (WB + Ozon).
Один Python процесс на Railway. Репо: `titboan/ai-office` (приватный). ОС разработчика: Windows / PowerShell.

---

## Порядок чтения в начале сессии

1. `CLAUDE.md` (этот файл) — правила, инструменты, навигация
2. `ai-clone/feedback/` — накопленные правила работы с Борисом (если папка есть)
3. `plans/` — если есть файл со строкой `Статус: в работе`, показать список и спросить продолжать
4. Зона задачи: нужный `.claude/skills/*/SKILL.md` или файл агента

---

## Структура репозитория

```
ai-office/
├── CLAUDE.md              ← этот файл
├── ROADMAP.md             ← долгосрочная дорожная карта проекта
├── config.py              ← конфиг, переменные окружения, CONSTANTS в конце файла
├── task_queue.py          ← логика очереди Postgres
├── main.py                ← точка входа, регистрация агентов, PTB setup
├── agents/
│   ├── base_agent.py      ← базовый класс агента, tool loop
│   └── marta.py, kevin.py, max.py, peter.py, elina.py, alex.py, dan.py, eva.py, kasper.py
├── tools/                 ← github.py, notion.py, ntfy.py, search.py, tg_status.py
├── utils/                 ← tg_format.py и утилиты
├── plans/                 ← технические планы (один план = одна фича)
├── retrospectives/        ← рефлексии после каждой сессии
├── ai-clone/feedback/     ← накопленные правила работы
├── Dockerfile, railway.toml, requirements.txt
└── .claude/skills/        ← SKILL.md для Claude Code
```

### Где что искать

| Что нужно | Куда смотреть |
|---|---|
| WB/Ozon API, Performance, парсеры | `.claude/skills/max-api/SKILL.md` |
| Схема БД, таблицы, VIEW | `.claude/skills/db-schema/SKILL.md` |
| Деплой Railway, переменные, отладка | `.claude/skills/deploy-railway/SKILL.md` |
| Аналитика Питера, ДРР, рентабельность | `.claude/skills/peter-analytics/SKILL.md` |
| Точка входа, регистрация агентов | `main.py` |
| Базовый класс агента, tool loop | `agents/base_agent.py` |
| Логика очереди Postgres | `task_queue.py` |
| Технические планы фич | `plans/` |
| Рефлексии сессий | `retrospectives/` |

---

## Инструменты

Python 3.11, PTB 22.7, asyncio, asyncpg, aiohttp · Redis (кеш, TTL, locks) · PostgreSQL Railway (task queue, orders, stocks, reviews, adv stats) · Claude API claude-sonnet-4-6 tool_use · Railway (деплой) · Notion API · DataLens (дашборды)

---

## Команда агентов

| Агент | key | Роль |
|-------|-----|------|
| 👩‍💼 Марта | marta | Координатор, chain planner, router |
| 👨‍💻 Кевин | kevin | Разработчик, GitHub API |
| 🔍 Каспер | kasper | Исследования, Tavily |
| 📊 Питер | peter | Бизнес-аналитик, отчёты |
| ✍️ Элина | elina | Копирайтер, Notion Content |
| 🗓️ Алекс | alex | Планировщик, ntfy.sh |
| 🎨 Дэн | dan | Дизайнер, Pollinations.ai |
| 📰 Ева | eva | Дайджест каналов (ждёт TELETHON_SESSION) |
| 🛒 Макс | max | Маркетплейс WB+Ozon |

---

## Архитектура

```
Telegram → Марта (router + chain planner)
├── одиночная задача → enqueue_task() → агент
└── цепочка → Claude API → JSON план → [подтверждение] → выполнение
Postgres Tasks: queued → running → completed/failed (chain_id, priority, retry)
```

Фоновые задачи: Макс — отзывы 09:00/14:00/20:00, негатив каждые 15 мин, реклама 03:00 UTC · Марта — статус Notion каждую минуту · Ева — дайджест 09:30 МСК

---

## Принципы (нарушать только с явным обоснованием)

1. **Один Python процесс** — никаких микросервисов
2. **Postgres — источник истины** — задачи, состояния, результаты только там
3. **Notion — UI**, не transactional backend
4. **Агенты изолированы** — только через очередь, никаких прямых вызовов
5. **Человек подтверждает** (в сессии Claude Code): `railway up`, PR merge, разовые write-запросы к WB/Ozon API. Автоматические фоновые задачи Макса — не требуют.
6. **Простота важнее фреймворков** — никаких LangGraph, CrewAI, Celery, DSPy
7. **Эволюция, не переписывание** — если нужно изменить >40% файла или сломать публичный интерфейс, сначала спроси

---

## Правила репо

1. `git add` поимённо — никогда `git add .` или `git add -A`
2. Перед `git push` проверить `git log origin/main..main --oneline`
3. `.env` не читать целиком — `Select-String "^VAR=" .env` для одной переменной
4. Не хардкодить динамические данные (цены, лимиты API, расписания) — в `config.py` в секцию `CONSTANTS` в конце файла
5. Если нужная папка не существует (`retrospectives/` и др.) — создай и продолжай

---

## Большая фича = план в `plans/`

Создай план если: затронуто 2+ файла, нужен новый эндпоинт / агент / таблица, или ты сам не уверен в объёме. Сомневаешься — создай план.

Если нет файла со статусом `в работе` — перед стартом создай план для текущей задачи.

Формат файла:
- Имя: `YYYY-MM-DD-название-задачи.md`
- Фазы с чек-боксами `[ ]` / `[x]`
- Строка `Статус: в работе / завершён`

После завершения всех фаз — поменять статус на `завершён`.

---

## Цикл в конце каждой фичи

1. Найди ошибки → докажи примером → исправь
2. Что упустили: граничные случаи, валидация входных данных, обработка ошибок
3. Реальная проблема нашего проекта? Сверь с `plans/`
4. Свежий взгляд — перечитай diff как будто видишь впервые
5. Security review: `.env`, API-ключи, персональные данные пользователей

Фича готова к пушу когда: цикл пройден, план обновлён `[x]`, PR создан.
Если остался известный edge case — создай issue в GitHub, не держи PR.

---

## Рефлексия в конце сессии

В `retrospectives/YYYY-MM-DD_краткое-название.md`:
1. Какая задача была
2. Как решали
3. Решили — да / нет / частично
4. Что можно было лучше
5. Что узнал нового о проекте или пользователе
6. Нарушил ли агент правило молча? → оформить в `ai-clone/feedback/` (новый .md файл), и если нужно — добавить в `.claude/settings.json` deny

---

## Известные баги (не трогать без надобности)

- `WB /adv/v1/promotion/adverts` → 404 с окт 2025 — названия кампаний вносить вручную в `wb_campaigns`
- `Notion Unclosed connection` — некритично, мониторим
- `Conflict` при деплое — норма, rolling restart, проходит за 30–60 сек

---

## Язык

Всегда отвечай на русском.
