# Дочистка max.py — Markdown → HTML

Статус: завершён

## Контекст

После перехода на HTML (план `2026-06-12-telegram-html-formatting.md`) в `max.py` остались 19 строк с Markdown-разметкой (`*текст*`), отправляемых с `parse_mode="HTML"`. Telegram в HTML-режиме игнорирует звёздочки — заголовки секций отображались без жирного.

---

## Фаза 1: Импорт

- [x] Добавить `from utils.tg_format import bold, escape` в `agents/max.py`

## Фаза 2: Исправление форматирования

- [x] `_send_sales_summary()` — заголовок статистики и 4 заголовка дат (`*...*` → `<b>...</b>`)
- [x] `_render_low()` / `_render_zero()` — имена товаров (`*{info['name']}*` → `<b>{escape(info['name'])}</b>`)
- [x] `_send_wb_stocks()` — заголовок + метки «Заканчиваются» / «Закончились»
- [x] `_send_ozon_stocks()` — то же самое
- [x] Callback `show_stats` — «Отзывы за сегодня»
- [x] Callback `daily_summary` — «Сводка магазина»
- [x] `cmd_shops` — «Ваши магазины»
- [x] `cmd_reviews` — «Отзывы за сегодня»
- [x] Инструкция по токену WB — «Statistics API» и «Статистика»

## Проверка

1. `grep -n "\*[^*]\+\*" agents/max.py` — не должно быть строк с `*текст*` в сообщениях
2. Вызвать `/start` у Макса — жирные заголовки в меню
3. Запросить сводку — жирные даты в статистике продаж
4. Запросить остатки WB/Ozon — жирные заголовки секций
5. Вызвать `/shops`, `/reviews` — жирные заголовки
