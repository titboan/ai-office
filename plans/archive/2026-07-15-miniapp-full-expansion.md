# Мини-апп: вкладки, отчёты, каталог, формы вместо мастеров Макса

Статус: завершён (2026-07-15) — все 5 фаз реализованы, продиффены и запушены в
`claude/telegram-miniapp-optimization-p0n0tb`.

## Контекст

Заменяет `plans/2026-07-15-miniapp-reports-expansion.md` — скоуп расширен по итогам повторного
разбора всего проекта (не только Питера) глазами нового клиента.

Решения с Борисом (2026-07-15):
- Текстовые отчёты Питера **не убираем** из чата — короткое summary + кнопка «Открыть в
  дашборде» вместо дублирования логики (передумали с полного дублирования, которое обсуждали
  раньше — риск рассинхрона двух реализаций одних расчётов).
- Мини-апп получает вкладки/разделы вместо одного скролла.
- Делаем всё: SEO, поставки, каталог, формы вместо мастеров.
- Single-entry-через-Марту — **уже завершено** (коммит `d54556f`, 2026-07-14): polling выключен
  у всех ботов кроме Марты, `ROADMAP.md` обновлён отдельным коммитом. Это условие для форм ниже —
  все новые эндпоинты валидируют `X-Telegram-Init-Data` строго по `MARTA_BOT_TOKEN`
  (`main.py::_validate_init_data`), тот же принцип, что уже у `applyPrice`/`applyBid`/`setCost`.

Разбор находок — что переносим и почему:

| Функция сейчас (чат) | Проблема | Решение |
|---|---|---|
| `/seo_audit` — текст | Нет визуала, данные (`kw_top`) уже объявлены в API, не заполняются | Вкладка «Отчёты» + summary в чате |
| `/supply` — текст | Нет визуала вообще | Вкладка «Отчёты» + summary в чате |
| `/audit` — текст (SWOT) | Прозаический анализ, плохо ложится в виджет | **Не переносим**, остаётся как есть |
| `/products`, `/margin`, `/shop_kpi` | Текстовые простыни, данные уже структурированы в БД | Вкладка «Каталог» |
| `/map`, `/cost` (частично) | `key=value` синтаксис командной строки в чате | Форма в мини-аппе |
| `/add` (`cmd_add`) | Пошаговый Redis-wizard (название→категория→площадка) через чат | Форма (все поля сразу) |
| `/merge_products` | Постраничный inline-кнопочный пикер | Форма-подбор (dropdown/список) |
| `/add_shop` | Ввод API-токена текстом в чат | Форма (чувствительные поля, не логировать) |

Не в скоупе: `/analyze` (свободный вопрос — годится только чат), генеративный контент Элины
(`/write`/`/seo`/`/post` — результат для копирования, чат — правильный формат), тендеры Тины
(низкая частота, не приоритет), approve/reject-кнопки Макса/Питера (`/apply_prices`,
`/bid_adjust`, `/campaigns` — уже хороший паттерн, не трогаем).

Реализация — по фазам, каждая отдельным вызовом `implementer` (см. `CLAUDE.md` — оркестрация).
После каждой фазы: сам читаю diff, коммичу, только потом следующая фаза.

---

## Фаза 1 — Вкладки в мини-аппе (инфраструктура) — ЗАВЕРШЕНА (2026-07-15)

- [x] `dashboard/src/App.tsx`: навигация по разделам — `Дашборд` (текущие 15 карточек как есть),
  `Отчёты` (новое, пусто до Фазы 2-3), `Каталог` (новое, пусто до Фазы 4), `Настройки`
  (`CostEditor` сюда).
- [x] Не ломать `useMainButtonAction`/`Telegram.WebApp` инициализацию — MainButton должен
  работать независимо от активной вкладки.
- [x] Мобильный вид (одна колонка) не менять функционально — только группировка по вкладкам.

## Фаза 2 — SEO-раздел — ЗАВЕРШЕНА (2026-07-15)

- [x] `kw_top` заполняется через `PeterAgent._collect_data()` (не `main.py` — `_handle_dashboard`
  уже спредит результат `_collect_data()` целиком). Правило приоритета — не из `cmd_seo_audit`
  (там другая метрика, urgency по CTR карточек), а то же, что у `/seo_check`
  (`agents/max.py::_check_seo_drops`, `config.SEO_POSITION_DROP_THRESHOLD` = 10 мест).
- [x] `dashboard/src/charts/KwTable.tsx` — таблица с подсветкой просевших позиций.
- [x] `agents/peter.py` `cmd_seo_audit`: сокращён до summary (топ-3 проблемных карточки),
  Claude-вызов убран (данные уже готовый список, LLM только форматировал) + кнопка на дашборд.

## Фаза 3 — Раздел «Поставки» — ЗАВЕРШЕНА (2026-07-15)

- [x] `_collect_supply_data` уже была переиспользуемой функцией; классификация срочности
  вынесена в `_classify_supply_urgency` (общий метод для чата и дашборда).
- [x] Отдельный эндпоинт не понадобился — `/api/dashboard` не имел кеша вообще (проверено),
  агрегат бьёт только Postgres + 2 запроса, уже кешированных в Redis на 6ч → поле `supply_plan`
  в `_collect_data()`, тот же путь, что `kw_top`.
- [x] `dashboard/src/charts/SupplyPlan.tsx` — план по регионам/кластерам, группировка по
  срочности.
- [x] `agents/peter.py` `cmd_supply`: summary (топ-5 срочных) + кнопка на дашборд, Claude-вызов
  убран (арифметика уже в Python, LLM только форматировал).

Замечено, не исправлено (вне рамок фазы): `/api/dashboard` не кеширован вообще — с ростом числа
полей стоит добавить общий Redis-кеш на `_handle_dashboard`, если дашборд начнёт тормозить.

## Фаза 4 — Раздел «Каталог» — ЗАВЕРШЕНА (2026-07-15)

- [x] `_get_catalog_products`/`_compute_margin_rows` (`agents/max.py`) — общие методы, маржа
  считается 1:1 как раньше, используются меню/чатом/дашбордом.
- [x] Данные — поле `catalog` в `_collect_catalog_for_dashboard` (Макс, не Питер — товары/цены/
  KPI живут у него), вызывается из `_handle_dashboard`, кэш Redis 5 мин (KPI бьёт живой API).
- [x] `dashboard/src/charts/ProductsTable.tsx`, `ShopKpiCard.tsx`.
- [x] `/products`, `/margin` — summary + кнопка на «Каталог». `/shop_kpi` не тронут — уже
  компактный, не «простыня».
- [x] Не дублирует `/api/costs` (₽-суммы себестоимости) и `NetMarginTable` (маржа с
  рекомендованными ценами) — только флаги `has_cost_*` и текстовая ссылка на вкладку
  «Дашборд».

## Фаза 5 — Формы вместо мастеров Макса — ЗАВЕРШЕНА (2026-07-15)

- [x] `/api/product`, `/api/merge_product`, `/api/add_shop` — тот же паттерн валидации
  `X-Telegram-Init-Data` по `MARTA_BOT_TOKEN`, что у `/api/set_cost`. SQL 1:1 из `/map`,
  `mergewiz:confirm`, `/add_shop`. Токен `/add_shop` — только JSON body, password-поле,
  не логируется даже в except (только тип исключения), rate limit 5/60.
- [x] `dashboard/src/charts/ProductForm.tsx`, `MergeProductForm.tsx`, `AddShopForm.tsx` — в
  разделах «Каталог»/«Настройки».
- [x] Старые команды `/map`, `/add`, `/merge_products`, `/add_shop` не тронуты, работают как
  раньше — добавлена ненавязчивая подсказка про дашборд после успеха.
- [ ] Мёртвые `ConversationHandler`/Redis-wizard регистрации в `agents/max.py` — сознательно НЕ
  удалены в этой фазе (см. пункт про отдельный заход в `ROADMAP.md`, Phase 10 «Осталось»).

Развилка, решена по факту кода, а не по букве плана: `/api/merge_product` — не простой
`UPDATE ozon_offer_id`, а полноценный `merge_product_rows` (перенос `product_costs`,
штрихкодов, удаление дублирующей строки) — воспроизводит реальный `mergewiz:confirm`, а не
предполагаемое упрощённое поведение.

---

## Цикл после каждой фазы (per `CLAUDE.md`)

Найти ошибки на примере → граничные случаи → сверить с этим планом → свежий взгляд на diff →
security review (токены `/add_shop` особенно) → если менялся формат чат-ответа — сверить
`parse_mode=`/`reply_text` на всех путях.
