# Единый диспетчер — Марта как главный интерфейс

Статус: завершён

## Задача

Снизить когнитивную нагрузку от 10 ботов. Пользователь пишет только Марте,
получает консолидированный результат. HTML-форматирование работает везде.

## Что сделано

### [x] HTML parse_mode — корневой фикс
- `base_agent.handle_message` line 463: добавлен `parse_mode="HTML"` с fallback
- `marta._process_text`: `reply_func(marta_response, parse_mode="HTML")` для всех веток
- `marta.cmd_delegate`: результат с `parse_mode="HTML"` + fallback
- HTML правила добавлены в MARTA_SYSTEM, KEVIN_SYSTEM, DAN_SYSTEM

### [x] Единый /help у Марты
- Показывает всю команду: возможности, примеры запросов
- 8 агентов описаны с примерами на русском
- Заменяет необходимость помнить 10 ботов

### [x] Proxy-команды в Марте
- `/report` → Питер (отчёт по продажам)
- `/reviews` → Макс (обработка отзывов)
- `/sync` → Макс (синхронизация данных)
- `/research` → Каспер (исследование)
- `/write` → Элина (текст)
- `/remind` → Алекс (напоминание)
- Зарегистрированы в BotFather через `_bot_commands()`

### [x] Уведомление при делегировании
- При одиночной задаче Марта присылает: "⏳ Передала задачу 📊 Питер — пришлю результат"
- Раньше Марта просто молчала после enqueue_task

### [x] Chain result consolidation
- Финальное сообщение цепочки теперь включает 200-симв. выжимку от каждого агента
- Форматирование переведено с Markdown на HTML
- Ссылки на Notion/GitHub — через `<a href>`

### [x] Notion — убран status_page loop (был убран ранее, подтверждено)

### [x] ROADMAP.md
- Дата обновления: 2026-06-14
- Phase 7 (Единый диспетчер) помечена завершённой
- Phase 8 (AI OS) и Phase 9 (Финансовая автоматизация) добавлены

### [x] Архив планов
- 13 выполненных планов перемещены в plans/archive/
- В активных остались: tina-tender-agent, tina-zapusk, eva-личный-секретарь
