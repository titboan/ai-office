# Аудит и чистка проекта ai-office

## Контекст
Накопившийся технический долг после серии крупных фич (акции, воронка, ABC, инфографика, SEO).
Цель: убрать мёртвый код, починить скрытый баг при shutdown, архивировать завершённые планы.
Принцип: минимальный точечный фикс, без рефакторинга рабочей логики.

---

## Фаза 1: Критический баг — shutdown crash

### `main.py` — RuntimeError при остановке сервиса
При graceful shutdown в цикл отмены задач попадают переменные `digest_task` и `negative_task`, которые **нигде не объявлены** в функции → `UnboundLocalError`.

- [x] Убрать необъявленные переменные из цикла отмены задач

---

## Фаза 2: Мёртвые импорты

### `main.py`
- [x] Удалить `from contextlib import suppress` (нигде не используется)
- [x] Удалить `DanAgent`, `EvaAgent` из импортов (закомментированы в `AGENTS`)

### `utils/tg_format.py`
- [x] Удалить дублированный импорт `RICH_MARKDOWN_FORMAT_RULES` (строка 16, есть ещё на 122)

---

## Фаза 3: Неиспользуемые параметры `pool` в task_queue.py

4 функции принимают `pool` но игнорируют его:
- [x] `enqueue_chain_task` — пометить параметр как `_pool=None`
- [x] `get_chain_results` — пометить параметр как `_pool=None`
- [x] `get_chain_status` — пометить параметр как `_pool=None`
- [x] `get_chain_plan` — пометить параметр как `_pool=None`

---

## Фаза 4: Дублирование вспомогательных функций

- [x] `base_agent.py` — удалить локальный `_strip_html()`, использовать из `utils.tg_format`
- [x] `main.py` — вынести `unique_chats` в хелпер `_unique_chats(shops)`
- [x] `main.py` — вынести `days_until_monday` в хелпер `_days_until_monday()`

---

## Фаза 5: Архив завершённых планов

- [x] Перенести 9 завершённых планов в `plans/archive/`

---

## Статус: завершён
