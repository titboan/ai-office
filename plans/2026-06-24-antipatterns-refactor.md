# Рефакторинг антипаттернов

Статус: в работе (Фазы 1-2 закрыты 2026-07-02; Фазы 3-4 сознательно отложены — делать
при следующем касании соответствующих файлов, см. «Порядок выполнения»)

## Контекст

Выявлено 6 антипаттернов в ходе аудита кодовой базы. Два из них дёшевы в исправлении и дают высокую отдачу — их закрываем в первую очередь. Остальные — по мере касания файлов.

---

## Фаза 1 — Единый реестр агентов (приоритет: высокий) [x]

**Проблема:** словари `{agent_key → name/emoji}` продублированы в 6+ местах:
`base_agent.py`, `marta.py` (×2), `tools/tg_status.py`, `tools/notion.py`, и внутри `_advance_chain`.
Добавление нового агента требует обновления всех мест вручную.

Сделано 2026-07-02. По факту дублей оказалось больше, чем описано в плане — `marta.py` содержал
не 2, а **6** копий (`_CHAIN_AGENT_EMOJI`/`_CHAIN_AGENT_NAMES` + 4×`_AGENT_EMOJI` в разных методах),
`base_agent.py` — 2 копии (в рендере чейн-результатов и отдельный модульный `_AGENT_NAMES`).
`tools/notion.py` не существует (Notion удалён в июне 2026, см. ROADMAP.md) — пункт снят.
Копии расходились между собой: у Тины в двух местах `marta.py` был эмодзи 🏛️ вместо канонического
📋 (`TinaAgent.emoji`), у Евы/Макса/Тины не было записи в `tools/tg_status.py._ALL_AGENTS` —
эти расхождения по пути исправлены.

- [x] Создать `agents/registry.py` с единым реестром (`AGENTS` + хелперы `agent_emoji`/
      `agent_name`/`agent_timeout`, плюс псевдо-агент `digest` для рендера чейн-логов)
- [x] Заменить дубли в `base_agent.py` (`_AGENT_NAMES` → функция `_agent_label()`,
      локальные `_AGENT_EMOJI`/`_AGENT_NAME` в рендере чейн-результатов → `agents/registry.py`)
- [x] Заменить дубли в `marta.py` (`_CHAIN_AGENT_EMOJI`/`_CHAIN_AGENT_NAMES`, 3× `_AGENT_EMOJI`
      в `cmd_status` и `mmenu_run`) — попутно исправлен эмодзи Тины и добавлены Ева/Макс/Тина
      туда, где их не хватало
- [x] Заменить дубли в `tools/tg_status.py` (`_AGENT_EMOJI`, `_AGENT_NAMES`, `_ALL_AGENTS`
      теперь `list(AGENTS.keys())`)
- [x] ~~`tools/notion.py`~~ — файла нет, Notion убран из проекта

---

## Фаза 2 — Таймаут агента из реестра (приоритет: высокий) [x]

**Проблема:** `timeout_seconds=600 if next_agent == "dan" else 300` хардкодом в 4 местах.
Хрупко: переименование агента или добавление нового медленного сломает тихо.

Сделано 2026-07-02. По факту в `marta.py` уже был единый `_task_timeout()` (не 4 разных
хардкода, как в плане — код успел эволюционировать после написания плана), но сам он
хардкодил `"dan" → 600`. Единственный оставшийся сырой хардкод был в `base_agent.py:1011`.

- [x] Добавить в `agents/registry.py` поле `timeout` (сделано в Фазе 1)
- [x] `base_agent.py` — `600 if next_agent == "dan" else 300` → `agent_timeout(next_agent)`
- [x] `marta.py._task_timeout()` — убран хардкод `"dan" → 600`, теперь берёт дефолт из
      `agent_timeout(agent_key)`, оставлен только особый случай тяжёлых синков Макса (900с)

---

## Фаза 3 — Убрать `pool=None` из сигнатур (приоритет: средний)

**Проблема:** `get_chain_plan(None, chain_id)` и `enqueue_chain_task(pool=None, ...)` — параметр `pool` врёт: `None` это нормальный рабочий путь, а не «не передан».

- [ ] В `task_queue.py`: убрать параметр `pool` из функций `get_chain_plan`, `enqueue_chain_task`, `get_chain_results`, `count_incomplete_in_group` — всегда вызывать `get_pool()` внутри
- [ ] Обновить все вызывающие места (в `base_agent.py` и `marta.py`)

> Делать при следующем касании `task_queue.py` по другой причине.

---

## Фаза 4 — Напоминания Алекса в BaseAgent (приоритет: низкий)

**Проблема:** `base_agent.py:690–714` — каждый агент (Марта, Питер, Макс…) при каждой 30-й итерации делает запрос к БД за напоминаниями. Это логика Алекса.

- [ ] Перенести блок `get_due_reminders` + `send_push` в `AlexAgent._worker_loop` (override с `await super()._worker_loop_tick()` или отдельным методом)

> Делать при следующем касании `alex.py` или `base_agent.py`.

---

## Не делать

- **Вынос `_advance_chain` в отдельный класс** — архитектурно правильно, но цена высокая, выгода нулевая пока цепочки не станут источником багов.
- **Переписывать dict-fallback для Redis** — в prod Redis всегда есть. Достаточно `logger.warning` при старте если Redis недоступен.

---

## Порядок выполнения

1. ~~Фаза 1 + Фаза 2~~ — сделано одним коммитом 2026-07-02 (реестр + таймауты)
2. Фаза 3 — отдельный PR при следующем касании `task_queue.py`
3. Фаза 4 — отдельный PR при следующем касании `alex.py`
