# Создать PR через gh CLI

Статус: завершён

## Контекст

Коммит `8b56f03` уже в `origin/main`. PR не создавался — фикс был влит напрямую в main.
`gh` CLI не установлен; при необходимости PR можно открыть через GitHub Web.

## Фазы

- [x] Выполнить команду создания PR
