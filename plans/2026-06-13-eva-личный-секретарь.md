# Ева — личный секретарь: Telegram-дайджест + дайджест почты

Статус: в работе
Дата: 2026-06-13

## Контекст

Ева уже написана (agents/eva.py) и умеет делать дайджест Telegram-каналов,
но заблокирована: TELETHON_SESSION не добавлен в Railway.
Задача: расширить Еву до личного секретаря — читать каналы + читать почту,
оба дайджеста приходят в Telegram в 09:30 МСК и по запросу.

---

## Фаза 1: Разблокировать Telegram-дайджест

- [ ] Добавить `TELETHON_SESSION` в Railway (ручная операция — Борис)
- [ ] Убедиться что заданы `EVA_BOT_TOKEN`, `TELEGRAM_API_ID`, `TELEGRAM_API_HASH`
- [ ] Проверить: `/add_channel @example` → `/digest 1d` → получить дайджест

*Код не меняется — только переменные окружения в Railway UI.*

---

## Фаза 2: Email-ридер

- [x] Создать `tools/email_reader.py` — async IMAP через stdlib `imaplib`
  - Поддержка Gmail (imap.gmail.com) и Яндекс (imap.yandex.ru)
  - `fetch_inbox_messages(host, user, password, since_days, max_messages)`
  - Парсинг: From, Subject, Date, body_preview (400 символов)
- [x] Добавить в `config.py`: `EMAIL_IMAP_HOST`, `EMAIL_USER`, `EMAIL_APP_PASS`

---

## Фаза 3: Email-дайджест в Еве

- [x] `_EMAIL_DIGEST_PROMPT` — группировка по категориям (🔴 Срочное / 💼 Работа / 🛒 Маркетплейсы / 📧 Рассылки / 📌 Прочее)
- [x] `run_email_digest(user_chat_id, since_days)` — IMAP → Claude → `_notify_user()`
- [x] `cmd_email_digest()` — команда `/email_digest [Nd]`
- [x] Регистрация в `_bot_commands()` и `_register_extra_handlers()`

---

## Фаза 4: Объединённый утренний дайджест

- [x] `main.py` `_scheduled_digest_loop`: 09:30 МСК запускает и Telegram-дайджест, и email-дайджест

---

## Фаза 5: UX личного секретаря

- [x] `EVA_SYSTEM` — обновлён до «личный секретарь»
- [x] `_help_text()` — обновлён: обе команды, расписание 09:30 МСК

---

## Переменные окружения Railway (добавить вручную)

**Для Telegram-дайджеста (Фаза 1):**
- `TELETHON_SESSION` — StringSession строка

**Для email-дайджеста:**
- `EMAIL_USER` — адрес почты (gmail или yandex)
- `EMAIL_APP_PASS` — пароль приложения (не основной пароль)
- `EMAIL_IMAP_HOST` — `imap.yandex.ru` для Яндекса (Gmail — по умолчанию)

---

## Проверка

1. `/add_channel @rbc_news` → `/digest 1d` → дайджест в чате
2. `/email_digest 1d` → сводка писем в Telegram по категориям
3. Дождаться 09:30 МСК → оба дайджеста автоматически
